
let today = "sunday"
if (today !== "monday" ||
    today !== "tuesday" ||
    today !== "wednesday" ||
    today !== "thursday" ||
    today !== "friday" ||
    today !== "saturday" ||
    today !== "sunday"
) console.log("there is no such day of week on Earth")
else if (today !== "sunday" && today !== "saturday") console.log("Go work!")

//(today !== "sunday" && today !== "saturday") ? console.log("Go work!") : console.log("You can sleep for a while")
